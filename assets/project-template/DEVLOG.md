# 开发日志（DEVLOG）

本项目的"代码留痕"文件：记录核心 idea、算法流程、代码计划，以及**每一次**写/改代码的原因。
目的——任何时候都能回溯"这段代码为什么这么写、是哪一步引入的"，方便复盘、交接与论文写作。

> 约定：每次新增或修改代码后，在第 4 节「变更记录」追加一行。先对齐（第 1–3 节），再编码。

## 1. 核心 idea / 要解决的问题

- **问题**：（一句话说清要解决什么）
- **核心想法（idea）**：（方法的关键创新点 / 假设）
- **对齐结论**：（动手前与导师/合作者确认的范围与目标；填日期）

## 2. 算法流程

端到端数据流与关键模块（先把 shape 标清楚，再写代码）：

```
输入  x: [B, ...]
  → 模块A  → ...: [B, ...]
  → 模块B  → ...
  → 输出  y_hat: [B, ...]
```

- 关键模块：（每个模块做什么 / 为什么需要）
- 目标函数：L = L_main + λ·L_aux （逐项说明，标注哪些权重是可配置/可消融的超参）
- 训练流程：（优化器、调度、特殊技巧）
- 评估指标：（任务对应的指标，如 PSNR/SSIM/Accuracy）

## 3. 代码计划（Plan）

- [ ] datasets/：数据集 + 预处理
- [ ] models/：模块A、模块B……
- [ ] losses/：各损失项
- [ ] trainers/：训练循环（如需自定义前向/调度）
- [ ] configs/：超参与可消融项
- [ ] 实验：基线 → 消融 → 分析

## 4. 变更记录（Changelog）

> 每次写/改代码追加一行。「为什么」比「改了什么」更重要——它解释了设计决策。

| 日期 | 改动 | 原因 | 涉及文件 |
|---|---|---|---|
| 2026-06-24 | 用脚手架初始化项目 | 起项目，建立模块化骨架 | 全部 |
